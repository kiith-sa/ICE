.. _modding_reference/component_ondeath:

=================
OnDeath component
=================

Currently, OnDeath component can't be used from YAML, only by internal game
logic. This might change in future. It handles custom logic that should be
executed when an entity dies.  Trying to use OnDeath component in YAML is
currently an error.
